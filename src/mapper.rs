// use mongodb::bson::Document;
// use cqrs_eventsourcing::FormatedEvent;

// // MAPPERS

// /// Converts Mongodb documents to FormatedEvents
// fn to_domain(docs: Vec<Document>) -> Vec<FormatedEvent> {
    
// }


// /// Converts FormatedEvents to Mongodb documents
// fn to_docs(events: Vec<FormatedEvent>) ->  Vec<Document>{

// }